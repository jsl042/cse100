

/*Terry Chu tjchu
Patrick Nguyen phn031*/

#include "countint.hpp"
#include <typeinfo>

class benchtree
{
  protected:
    const int numRuns;

  public:

  int main( int argc,const char *argv[]) 
  {
    if( argc < 4)
    {
      std::cout << "Not enough arguments try again" << std::endl;
      exit(0);
    }
  
    if( (argv[0] != "bst") || (argv[0] != "rst") || (argv[0] != "set"))
    {
      std::cout << 
     exit(0);
    } 

    if( (argv[1] != "sorted") || argv[1] != "shuffled" )
    {
      std::cout <<
      exit(0);
    }
    if( typeid(argv[2]) != "int" )
    {
      exit(0);
    }
    if( typeid(argv[3] != "int" ))
    {

    } 
    numRuns = argv[3];
   countin::clearcount();
   std::vector<countint>::iterator vit = v.begin();
   std::vector<countint>::iterator ven = v.end();
   for(vit )

  }


}
